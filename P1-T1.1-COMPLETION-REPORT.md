# P1-T1.1 에디터 레이아웃 완료 보고서

## 📋 태스크 정보

- **Phase**: 1
- **태스크 ID**: P1-T1.1
- **태스크명**: 에디터 레이아웃 (3단: TOC + Editor + Preview)
- **완료일**: 2026-01-28
- **담당**: Claude (Frontend Specialist)

---

## ✅ 완료된 작업

### 1. 파일 생성

| 파일 경로 | 용도 |
|----------|------|
| `src/components/editor/EditorLayout.tsx` | 3단 레이아웃 컴포넌트 (6.3KB) |
| `src/app/(host)/editor/[id]/page.tsx` | 에디터 페이지 (서버 컴포넌트) |
| `src/types/editor.ts` | 타입 정의 (Block, Guide, EditorState) |
| `tests/components/editor/EditorLayout.test.tsx` | 단위 테스트 |
| `src/app/demo/phase-1/t1-1-editor-layout/page.tsx` | 데모 페이지 |

### 2. 기능 구현

#### 3단 레이아웃 (Desktop)

- **TOC Sidebar** (240px 고정)
  - 블록 목록 표시
  - "블록 추가" 버튼
  - 선택된 블록 하이라이트
  - 스크롤 가능

- **Editor Panel** (flex-1)
  - 가이드북 제목 표시
  - 저장 상태 표시 ("저장됨")
  - 블록 에디터 영역 (플레이스홀더)
  - 스크롤 가능

- **Preview Panel** (375px 고정)
  - iPhone 프레임 스타일
  - 노치 (notch)
  - 홈 인디케이터
  - 블록 미리보기
  - 스크롤 가능

#### 반응형 레이아웃

| 화면 크기 | 레이아웃 | 동작 |
|----------|---------|------|
| Desktop (≥1024px) | 3단 (TOC + Editor + Preview) | 모든 패널 동시 표시 |
| Tablet (768-1023px) | 2단 (Editor + Preview) | TOC 숨김 |
| Mobile (<768px) | 1단 (탭 전환) | "편집" / "미리보기" 탭으로 전환 |

#### 상태 관리

- `activeTab`: 모바일 탭 상태 (editor/preview)
- `selectedBlockId`: 현재 선택된 블록 ID

### 3. 디자인 시스템 적용

| 요소 | 적용 |
|------|------|
| 색상 | Primary (#2563EB), Secondary (#F97316) |
| 배경 | bg-gray-50 (전체), bg-white (패널) |
| 간격 | gap-0 (레이아웃), gap-2 (내부 요소) |
| 그림자 | shadow-sm (TOC), shadow-xl (Preview) |
| Border Radius | rounded-lg (버튼), rounded-[40px] (iPhone) |

---

## ✅ AC (Acceptance Criteria) 검증

| AC | 상태 | 확인 |
|----|------|------|
| 3단 레이아웃 렌더링 | ✅ | `lg:grid-cols-[240px_1fr_375px]` 적용 |
| 반응형 동작 (모바일: 탭 전환) | ✅ | `role="tab"` + `activeTab` 상태 |
| TOC에 블록 추가 버튼 표시 | ✅ | `<Button>` + `<PlusIcon>` |
| Preview에 iPhone 프레임 표시 | ✅ | `iphone-frame` 클래스 + 노치/홈 인디케이터 |

---

## 🎨 데모 페이지

### 접근 경로

```
http://localhost:3000/demo/phase-1/t1-1-editor-layout
```

### 데모 상태

| 상태 | 설명 |
|------|------|
| `empty` | 빈 가이드북 (블록 0개) |
| `single` | 단일 블록 가이드북 |
| `normal` | 일반 가이드북 (블록 3개) |
| `many` | 다수 블록 가이드북 (블록 10개) |

---

## 🧪 검증 결과

### 타입 체크

```bash
npx tsc --noEmit
```

✅ 타입 에러 없음

### 린트 검사

```bash
npx eslint src/components/editor/EditorLayout.tsx
```

✅ 린트 에러 없음

### 파일 크기

- `EditorLayout.tsx`: 6.3KB
- `page.tsx`: 1.1KB

---

## 📚 타입 정의

```typescript
// src/types/editor.ts

export interface Block {
  id: string;
  type: string;
  order: number;
  data: Record<string, any>;
}

export interface Guide {
  id: string;
  title: string;
  slug: string;
  blocks: Block[];
}

export interface EditorState {
  selectedBlockId: string | null;
  isDirty: boolean;
  isSaving: boolean;
  lastSaved: Date | null;
}

export type SaveStatus = 'saved' | 'saving' | 'unsaved' | 'error';
```

---

## 🔄 다음 단계 (P1-T1.2)

1. **블록 에디터 구현**
   - 블록 타입별 에디터 (Hero, Info, WiFi, Map 등)
   - 실시간 미리보기 동기화

2. **TOC 드래그앤드롭**
   - 블록 순서 변경
   - `react-beautiful-dnd` 또는 `@dnd-kit` 사용

3. **저장 기능**
   - 자동 저장 (debounce)
   - 저장 상태 표시 업데이트

---

## 📝 코드 주요 포인트

### 1. 반응형 탭 전환

```tsx
// 모바일: 탭으로 Editor/Preview 전환
<button
  role="tab"
  aria-selected={activeTab === 'editor'}
  onClick={() => setActiveTab('editor')}
>
  편집
</button>

// 탭에 따라 패널 숨김/표시
<aside className={cn(
  'bg-white',
  activeTab === 'preview' && 'hidden lg:block'
)}>
```

### 2. iPhone 프레임

```tsx
<div className={cn(
  'iphone-frame',
  'w-full max-w-[375px]',
  'bg-white rounded-[40px] shadow-xl',
  'border-[12px] border-black',
  'relative overflow-hidden'
)}>
  {/* 노치 */}
  <div className="absolute top-0 left-1/2 -translate-x-1/2 w-40 h-6 bg-black rounded-b-3xl z-10" />

  {/* 스크린 */}
  <div className="h-[667px] overflow-y-auto bg-gray-50">
    {/* 블록 렌더링 */}
  </div>

  {/* 홈 인디케이터 */}
  <div className="absolute bottom-2 left-1/2 -translate-x-1/2 w-32 h-1 bg-gray-400 rounded-full" />
</div>
```

### 3. 블록 선택 상태

```tsx
const [selectedBlockId, setSelectedBlockId] = useState<string | null>(
  guide.blocks[0]?.id || null
);

// 블록 클릭 시 선택
<button
  onClick={() => setSelectedBlockId(block.id)}
  className={cn(
    'w-full text-left px-3 py-2 rounded-lg',
    selectedBlockId === block.id
      ? 'bg-primary-light border border-primary'
      : 'border border-transparent'
  )}
>
```

---

## 🎯 TAG System 적용

모든 파일에 `@TASK P1-T1.1` 및 `@SPEC` 주석 추가 완료.

---

## ✅ TASK_DONE

모든 AC 충족, 타입 체크 및 린트 통과, 데모 페이지 생성 완료.

**다음 작업**: P1-T1.2 블록 에디터 구현
