# Database Scripts

Vibe Store의 데이터베이스 마이그레이션 및 시드 스크립트입니다.

## 📋 스크립트 목록

| 스크립트 | 명령어 | 설명 |
|---------|--------|------|
| **migrate.ts** | `npm run db:migrate` | 마이그레이션 파일을 순차 실행 |
| **seed.ts** | `npm run db:seed` | 초기 시드 데이터 삽입 |
| **reset.ts** | `npm run db:reset` | DB 초기화 (삭제 + 시드) |

---

## 🚀 사용법

### 1. 마이그레이션 실행

```bash
npm run db:migrate
```

**기능:**
- Supabase 연결 테스트
- `supabase/migrations/*.sql` 파일을 순차 실행
- 실행 결과 로깅

**참고:**
- 현재 버전은 **안내 모드**입니다.
- Supabase CLI 또는 Dashboard에서 수동 실행을 권장합니다.

---

### 2. 시드 데이터 삽입

```bash
npm run db:seed
```

**시드 데이터:**
- 샘플 카테고리 4개
- 샘플 상품 4개
- 샘플 태그 8개
- 상품-태그 연결
- 샘플 이미지

---

### 3. 데이터베이스 초기화

```bash
npm run db:reset
```

**⚠️  경고:** 프로덕션 환경에서 절대 실행하지 마세요!

---

## 🔧 환경 설정

`.env.local` 파일에 다음 환경변수가 필요합니다:

```bash
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
```

---

## 🧪 테스트

```bash
npm run test -- tests/scripts/
```
