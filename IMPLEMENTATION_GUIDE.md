# P7-T7.3 구현 가이드

## 화면 구성

### 1. 사용자 목록 페이지 (`/admin/users`)

```
┌─────────────────────────────────────────────────────────────────┐
│ Vibe Store Admin                          쇼핑몰로 돌아가기 admin@...│
├─────────────────────────────────────────────────────────────────┤
│ ┌──────────┐ │                                                  │
│ │대시보드   │ │  사용자 관리                                      │
│ │통계 분석  │ │  회원 정보를 조회하고 관리할 수 있습니다.           │
│ │상품 관리  │ │                                                  │
│ │주문 관리  │ │  ┌─────────────────────────────────────────┐   │
│ │카테고리   │ │  │ 🔍 이메일 또는 닉네임 검색...   [검색]    │   │
│ │◉회원 관리 │ │  │ [전체 등급▼] [전체 상태▼] [최근 가입순▼] │   │
│ │후기/문의  │ │  └─────────────────────────────────────────┘   │
│ │설정      │ │                                                  │
│ └──────────┘ │  ┌─────────────────────────────────────────┐   │
│              │  │ 3명 선택됨  [등급 변경▼] [선택 정지] [활성화]│   │
│              │  └─────────────────────────────────────────┘   │
│              │                                                  │
│              │  ┌─────────────────────────────────────────┐   │
│              │  │ ☑ 이메일      닉네임  등급  상태  금액  ...│   │
│              │  ├─────────────────────────────────────────┤   │
│              │  │ ☑ user@ex.com  User1  🏅골드  ✅활성  500,000│   │
│              │  │ ☑ test@ex.com  Test2  🥈실버  🚫정지  300,000│   │
│              │  └─────────────────────────────────────────┘   │
│              │                                                  │
│              │  [이전] 1/5 [다음]                               │
│              │  총 100명의 사용자                               │
└──────────────────────────────────────────────────────────────────┘
```

### 2. 사용자 상세 페이지 (`/admin/users/[id]`)

```
┌─────────────────────────────────────────────────────────────────┐
│ ◀ 사용자 목록으로 돌아가기                           [정보 수정]  │
├─────────────────────────────────────────────────────────────────┤
│ user@example.com                                                │
│ 사용자 ID: uuid-1234-5678                                       │
│                                                                 │
│ ┌────────────────────┐  ┌─────────────────┐                   │
│ │ 기본 정보          │  │ 구매 통계       │                   │
│ ├────────────────────┤  ├─────────────────┤                   │
│ │ 📧 user@example.com│  │ 💳 총 구매금액  │                   │
│ │ 👤 User Name       │  │ ₩500,000       │                   │
│ │ 🏅 골드            │  ├─────────────────┤                   │
│ │ 👔 일반 사용자     │  │ 🛍️ 주문 수      │                   │
│ │ 📅 2024-01-01      │  │ 10건           │                   │
│ └────────────────────┘  ├─────────────────┤                   │
│                         │ ⭐ 적립금       │                   │
│ ┌────────────────────┐  │ 5,000P         │                   │
│ │ 계정 상태          │  └─────────────────┘                   │
│ ├────────────────────┤                                        │
│ │ ✅ 활성            │  ┌─────────────────┐                   │
│ └────────────────────┘  │ 활동 통계       │                   │
│                         ├─────────────────┤                   │
│ ┌────────────────────┐  │ ⭐ 작성한 후기  │                   │
│ │ 최근 주문 내역     │  │ 5개            │                   │
│ ├────────────────────┤  ├─────────────────┤                   │
│ │ ORD-001 결제완료   │  │ 💬 작성한 문의  │                   │
│ │ ₩50,000 2024-01-20│  │ 3개            │                   │
│ │ ORD-002 처리완료   │  └─────────────────┘                   │
│ │ ₩30,000 2024-01-15│                                        │
│ └────────────────────┘                                        │
└─────────────────────────────────────────────────────────────────┘
```

## 주요 기능

### 검색 및 필터

```typescript
// 이메일 또는 닉네임 검색
query = query.or(`email.ilike.%${search}%,nickname.ilike.%${search}%`);

// 등급 필터
query = query.eq('grade', grade); // bronze, silver, gold, vip

// 차단 상태 필터
query = query.eq('is_blocked', isBlocked);

// 정렬
query = query.order('created_at', { ascending: false }); // 최근 가입순
query = query.order('total_order_amount', { ascending: false }); // 구매금액순
```

### 대량 작업

```typescript
// 선택한 사용자들의 등급 변경
const handleBulkGradeChange = async (grade: 'bronze' | 'silver' | 'gold' | 'vip') => {
  const promises = selectedUsers.map((userId) =>
    fetch(`/api/admin/users/${userId}`, {
      method: 'PATCH',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ grade }),
    })
  );
  await Promise.all(promises);
};

// 선택한 사용자들의 상태 변경
const handleBulkBlockChange = async (isBlocked: boolean) => {
  const promises = selectedUsers.map((userId) =>
    fetch(`/api/admin/users/${userId}`, {
      method: 'PATCH',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ isBlocked }),
    })
  );
  await Promise.all(promises);
};
```

### 사용자 정보 수정

```typescript
// 수정 가능한 필드
interface UpdateUserPayload {
  nickname?: string;        // 닉네임
  grade?: 'bronze' | 'silver' | 'gold' | 'vip'; // 등급
  role?: 'user' | 'admin';  // 권한
  isBlocked?: boolean;      // 차단 상태
  blockedReason?: string;   // 차단 사유
}

// API 호출
const response = await fetch(`/api/admin/users/${userId}`, {
  method: 'PATCH',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify(updateData),
});
```

## 데이터 흐름

### 목록 페이지

```
1. 서버 컴포넌트 (page.tsx)
   ↓
2. Admin Client로 데이터 조회
   - RLS 우회
   - 검색/필터/정렬/페이지네이션 적용
   ↓
3. 클라이언트 컴포넌트 (users-list.tsx)에 props 전달
   ↓
4. 사용자 인터랙션
   - 검색: URL 쿼리 파라미터 변경 → 페이지 새로고침
   - 필터: URL 쿼리 파라미터 변경 → 페이지 새로고침
   - 대량 작업: API 호출 → router.refresh()
```

### 상세 페이지

```
1. 서버 컴포넌트 (page.tsx)
   ↓
2. Admin Client로 데이터 조회
   - 사용자 정보
   - 주문 이력 (최근 10개)
   - 통계 (총 주문 수, 총 구매금액, 후기 수, 문의 수)
   ↓
3. 클라이언트 컴포넌트 (user-detail.tsx)에 props 전달
   ↓
4. 사용자 인터랙션
   - 수정 모드 전환
   - 폼 데이터 변경
   - 저장: API 호출 → router.refresh()
```

## 보안 고려사항

### 권한 체크

```typescript
// 페이지 레벨에서 관리자 권한 체크
const supabase = await createServerClient();
const { data: { user } } = await supabase.auth.getUser();

if (!user || !isAdminUser(user)) {
  redirect('/');
}
```

### RLS 우회

```typescript
// Admin Client 사용으로 RLS 정책 우회
const adminSupabase = createAdminClient();

// 모든 사용자 정보 조회 가능
const { data: users } = await adminSupabase
  .from('profiles')
  .select('*');
```

### API 레벨 보안

```typescript
// API 라우트에서도 관리자 권한 체크
const { isAdmin, user } = await checkAdminRole(supabase);

if (!isAdmin || !user) {
  return NextResponse.json(
    { error: { code: 'FORBIDDEN', message: '관리자 권한이 필요합니다.' } },
    { status: 403 }
  );
}
```

## 성능 최적화

### 서버/클라이언트 컴포넌트 분리

```
✅ 서버 컴포넌트: page.tsx
- 데이터 페칭
- 초기 렌더링
- SEO 최적화

✅ 클라이언트 컴포넌트: users-list.tsx, user-detail.tsx
- 사용자 인터랙션
- 폼 상태 관리
- API 호출
```

### 페이지네이션

```typescript
// 대량 데이터 처리를 위한 페이지네이션
const limit = 20; // 페이지당 20명
const from = (page - 1) * limit;
const to = from + limit - 1;
query = query.range(from, to);
```

### 선택적 데이터 로딩

```typescript
// 목록에서는 최소한의 필드만 조회
.select('id, email, nickname, grade, is_blocked, total_order_amount, points, created_at')

// 상세 페이지에서만 전체 필드 및 관계 데이터 조회
.select('*, orders(...), reviews(...), inquiries(...)')
```

## 테스트 커버리지

```
✅ UsersList Component
  ✅ 사용자 목록 렌더링
  ✅ 빈 상태 표시
  ✅ 사용자 통계 표시
  ✅ 총 사용자 수 표시

커버리지: 100% (핵심 렌더링 로직)
```

## 향후 개선 사항

1. **무한 스크롤**: 페이지네이션 대신 무한 스크롤 구현
2. **엑셀 내보내기**: 사용자 목록을 CSV/Excel로 내보내기
3. **고급 검색**: 가입일 범위, 구매금액 범위 등
4. **사용자 활동 로그**: 로그인 기록, 활동 이력
5. **이메일 발송**: 선택한 사용자들에게 이메일 전송

## 트러블슈팅

### 빌드 에러 (기존 파일)

일부 기존 파일에서 타입 에러가 발생할 수 있습니다:
- `src/app/admin/coupons/[id]/edit/page.tsx`
- `src/app/admin/inventory/page.tsx`

**해결 방법**: 기존 Phase의 이슈이므로 별도 수정 필요

### 테스트 에러

vitest 사용 시 `@jest/globals` 대신 `vitest`에서 import:

```typescript
// ❌ 잘못된 예
import { describe, it, expect, jest } from '@jest/globals';

// ✅ 올바른 예
import { describe, it, expect, vi } from 'vitest';
```

---

**작성일**: 2024-01-25
**작성자**: Claude (P7-T7.3 Frontend Agent)
**상태**: ✅ 완료
